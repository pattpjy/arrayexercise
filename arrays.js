var names = ['donut','ice cream','coffee','tea'];
var codes = [1,2,3,4];
var stocks = [true,false,true,true];
console.log(stocks);

// pop remove the last element in the names array
names.pop();
// push add lenght to array
codes.push(5);
//shift removes the first element from an array and returns that removed element.
stocks.shift();

console.log(names);
console.log(codes);
console.log(stocks);
